{
  "$schema": "http://json-schema.org/schema",
  "id": "generate-fastify-app",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the app to create",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
